import { Button, Input } from 'antd';

const Home = () => {
  return (
    <div>
      Home Page <Button>Click</Button>
      <Input placeholder={'jhasgfjhg'} />
    </div>
  );
};

export default Home;
