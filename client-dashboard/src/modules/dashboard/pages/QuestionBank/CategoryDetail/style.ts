import styled from 'styled-components';

export const CategoryDetailWrapper = styled.div`
  overflow: scroll;
  .CategoryDetail {
    &__body {
      padding: 1.5rem;
      .ant-table-row {
        cursor: pointer;
      }
    }
  }
`;
